"""Agent Plugins 1.0.0 packaging.

Agent Plugins is a *distribution* format, not an enforcement surface, and the distinction is
load-bearing. See `build.py` for why this package deliberately does not touch
`compile.surfaces`.
"""
