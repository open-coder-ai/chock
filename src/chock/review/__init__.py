"""Reviewer evidence: what a review of a change rests on.

`verified` claims are re-derived by whoever reads them. `attested` claims are a named
reviewer's judgement. The format's job is keeping the two apart -- see `evidence.py`.
"""
